/*
 * LoginRadius API
 * This is a LoginRadius API server. Which you can use to authenticate and manage the user.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@loginradius.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.loginradius.sdk.internal.openapi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.loginradius.sdk.internal.openapi.model.BulkInsertErrorReport;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.loginradius.sdk.internal.openapi.JSON;

/**
 * BulkInsertReport
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.10.0")
public class BulkInsertReport {
  public static final String SERIALIZED_NAME_RECORD_INSERTED = "RecordInserted";
  @SerializedName(SERIALIZED_NAME_RECORD_INSERTED)
  @javax.annotation.Nonnull
  private Long recordInserted;

  public static final String SERIALIZED_NAME_RECORD_UPDATED = "RecordUpdated";
  @SerializedName(SERIALIZED_NAME_RECORD_UPDATED)
  @javax.annotation.Nonnull
  private Long recordUpdated;

  public static final String SERIALIZED_NAME_FAILED = "Failed";
  @SerializedName(SERIALIZED_NAME_FAILED)
  @javax.annotation.Nullable
  private List<BulkInsertErrorReport> failed = new ArrayList<>();

  public BulkInsertReport() {
  }

  public BulkInsertReport recordInserted(@javax.annotation.Nonnull Long recordInserted) {
    this.recordInserted = recordInserted;
    return this;
  }

  /**
   * Number of records successfully inserted.
   * @return recordInserted
   */
  @javax.annotation.Nonnull
  public Long getRecordInserted() {
    return recordInserted;
  }

  public void setRecordInserted(@javax.annotation.Nonnull Long recordInserted) {
    this.recordInserted = recordInserted;
  }


  public BulkInsertReport recordUpdated(@javax.annotation.Nonnull Long recordUpdated) {
    this.recordUpdated = recordUpdated;
    return this;
  }

  /**
   * Number of records successfully updated.
   * @return recordUpdated
   */
  @javax.annotation.Nonnull
  public Long getRecordUpdated() {
    return recordUpdated;
  }

  public void setRecordUpdated(@javax.annotation.Nonnull Long recordUpdated) {
    this.recordUpdated = recordUpdated;
  }


  public BulkInsertReport failed(@javax.annotation.Nullable List<BulkInsertErrorReport> failed) {
    this.failed = failed;
    return this;
  }

  public BulkInsertReport addFailedItem(BulkInsertErrorReport failedItem) {
    if (this.failed == null) {
      this.failed = new ArrayList<>();
    }
    this.failed.add(failedItem);
    return this;
  }

  /**
   * List of records that failed to insert or update.
   * @return failed
   */
  @javax.annotation.Nullable
  public List<BulkInsertErrorReport> getFailed() {
    return failed;
  }

  public void setFailed(@javax.annotation.Nullable List<BulkInsertErrorReport> failed) {
    this.failed = failed;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the BulkInsertReport instance itself
   */
  public BulkInsertReport putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BulkInsertReport bulkInsertReport = (BulkInsertReport) o;
    return Objects.equals(this.recordInserted, bulkInsertReport.recordInserted) &&
        Objects.equals(this.recordUpdated, bulkInsertReport.recordUpdated) &&
        Objects.equals(this.failed, bulkInsertReport.failed)&&
        Objects.equals(this.additionalProperties, bulkInsertReport.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(recordInserted, recordUpdated, failed, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class BulkInsertReport {\n");
    sb.append("    recordInserted: ").append(toIndentedString(recordInserted)).append("\n");
    sb.append("    recordUpdated: ").append(toIndentedString(recordUpdated)).append("\n");
    sb.append("    failed: ").append(toIndentedString(failed)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("RecordInserted");
    openapiFields.add("RecordUpdated");
    openapiFields.add("Failed");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("RecordInserted");
    openapiRequiredFields.add("RecordUpdated");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to BulkInsertReport
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!BulkInsertReport.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in BulkInsertReport is not found in the empty JSON string", BulkInsertReport.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : BulkInsertReport.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("Failed") != null && !jsonObj.get("Failed").isJsonNull()) {
        JsonArray jsonArrayfailed = jsonObj.getAsJsonArray("Failed");
        if (jsonArrayfailed != null) {
          // ensure the json data is an array
          if (!jsonObj.get("Failed").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `Failed` to be an array in the JSON string but got `%s`", jsonObj.get("Failed").toString()));
          }

          // validate the optional field `Failed` (array)
          for (int i = 0; i < jsonArrayfailed.size(); i++) {
            BulkInsertErrorReport.validateJsonElement(jsonArrayfailed.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!BulkInsertReport.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'BulkInsertReport' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<BulkInsertReport> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(BulkInsertReport.class));

       return (TypeAdapter<T>) new TypeAdapter<BulkInsertReport>() {
           @Override
           public void write(JsonWriter out, BulkInsertReport value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public BulkInsertReport read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             BulkInsertReport instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of BulkInsertReport given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of BulkInsertReport
   * @throws IOException if the JSON string is invalid with respect to BulkInsertReport
   */
  public static BulkInsertReport fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, BulkInsertReport.class);
  }

  /**
   * Convert an instance of BulkInsertReport to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}


/*
 * LoginRadius API
 * This is a LoginRadius API server. Which you can use to authenticate and manage the user.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@loginradius.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.loginradius.sdk.internal.openapi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.loginradius.sdk.internal.openapi.JSON;

/**
 * IPAccessRestrictions
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.10.0")
public class IPAccessRestrictions {
  public static final String SERIALIZED_NAME_ALLOWED_I_PS = "AllowedIPs";
  @SerializedName(SERIALIZED_NAME_ALLOWED_I_PS)
  @javax.annotation.Nullable
  private List<String> allowedIPs = new ArrayList<>();

  public static final String SERIALIZED_NAME_DENIED_I_PS = "DeniedIPs";
  @SerializedName(SERIALIZED_NAME_DENIED_I_PS)
  @javax.annotation.Nullable
  private List<String> deniedIPs = new ArrayList<>();

  public IPAccessRestrictions() {
  }

  public IPAccessRestrictions allowedIPs(@javax.annotation.Nullable List<String> allowedIPs) {
    this.allowedIPs = allowedIPs;
    return this;
  }

  public IPAccessRestrictions addAllowedIPsItem(String allowedIPsItem) {
    if (this.allowedIPs == null) {
      this.allowedIPs = new ArrayList<>();
    }
    this.allowedIPs.add(allowedIPsItem);
    return this;
  }

  /**
   * List of allowed IP addresses
   * @return allowedIPs
   */
  @javax.annotation.Nullable
  public List<String> getAllowedIPs() {
    return allowedIPs;
  }

  public void setAllowedIPs(@javax.annotation.Nullable List<String> allowedIPs) {
    this.allowedIPs = allowedIPs;
  }


  public IPAccessRestrictions deniedIPs(@javax.annotation.Nullable List<String> deniedIPs) {
    this.deniedIPs = deniedIPs;
    return this;
  }

  public IPAccessRestrictions addDeniedIPsItem(String deniedIPsItem) {
    if (this.deniedIPs == null) {
      this.deniedIPs = new ArrayList<>();
    }
    this.deniedIPs.add(deniedIPsItem);
    return this;
  }

  /**
   * List of blocked IP addresses
   * @return deniedIPs
   */
  @javax.annotation.Nullable
  public List<String> getDeniedIPs() {
    return deniedIPs;
  }

  public void setDeniedIPs(@javax.annotation.Nullable List<String> deniedIPs) {
    this.deniedIPs = deniedIPs;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the IPAccessRestrictions instance itself
   */
  public IPAccessRestrictions putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IPAccessRestrictions ipAccessRestrictions = (IPAccessRestrictions) o;
    return Objects.equals(this.allowedIPs, ipAccessRestrictions.allowedIPs) &&
        Objects.equals(this.deniedIPs, ipAccessRestrictions.deniedIPs)&&
        Objects.equals(this.additionalProperties, ipAccessRestrictions.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(allowedIPs, deniedIPs, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class IPAccessRestrictions {\n");
    sb.append("    allowedIPs: ").append(toIndentedString(allowedIPs)).append("\n");
    sb.append("    deniedIPs: ").append(toIndentedString(deniedIPs)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("AllowedIPs");
    openapiFields.add("DeniedIPs");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to IPAccessRestrictions
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!IPAccessRestrictions.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in IPAccessRestrictions is not found in the empty JSON string", IPAccessRestrictions.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("AllowedIPs") != null && !jsonObj.get("AllowedIPs").isJsonNull() && !jsonObj.get("AllowedIPs").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `AllowedIPs` to be an array in the JSON string but got `%s`", jsonObj.get("AllowedIPs").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("DeniedIPs") != null && !jsonObj.get("DeniedIPs").isJsonNull() && !jsonObj.get("DeniedIPs").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `DeniedIPs` to be an array in the JSON string but got `%s`", jsonObj.get("DeniedIPs").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!IPAccessRestrictions.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'IPAccessRestrictions' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<IPAccessRestrictions> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(IPAccessRestrictions.class));

       return (TypeAdapter<T>) new TypeAdapter<IPAccessRestrictions>() {
           @Override
           public void write(JsonWriter out, IPAccessRestrictions value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public IPAccessRestrictions read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             IPAccessRestrictions instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of IPAccessRestrictions given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of IPAccessRestrictions
   * @throws IOException if the JSON string is invalid with respect to IPAccessRestrictions
   */
  public static IPAccessRestrictions fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, IPAccessRestrictions.class);
  }

  /**
   * Convert an instance of IPAccessRestrictions to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}


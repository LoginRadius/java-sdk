/*
 * LoginRadius API
 * This is a LoginRadius API server. Which you can use to authenticate and manage the user.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@loginradius.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.loginradius.sdk.internal.openapi.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.loginradius.sdk.internal.openapi.JSON;

/**
 * DomainAccessRestrictions
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.10.0")
public class DomainAccessRestrictions {
  public static final String SERIALIZED_NAME_ALLOWLIST = "Allowlist";
  @SerializedName(SERIALIZED_NAME_ALLOWLIST)
  @javax.annotation.Nullable
  private List<String> allowlist = new ArrayList<>();

  public static final String SERIALIZED_NAME_BLOCKLIST = "Blocklist";
  @SerializedName(SERIALIZED_NAME_BLOCKLIST)
  @javax.annotation.Nullable
  private List<String> blocklist = new ArrayList<>();

  public DomainAccessRestrictions() {
  }

  public DomainAccessRestrictions allowlist(@javax.annotation.Nullable List<String> allowlist) {
    this.allowlist = allowlist;
    return this;
  }

  public DomainAccessRestrictions addAllowlistItem(String allowlistItem) {
    if (this.allowlist == null) {
      this.allowlist = new ArrayList<>();
    }
    this.allowlist.add(allowlistItem);
    return this;
  }

  /**
   * List of allowed domains/emails
   * @return allowlist
   */
  @javax.annotation.Nullable
  public List<String> getAllowlist() {
    return allowlist;
  }

  public void setAllowlist(@javax.annotation.Nullable List<String> allowlist) {
    this.allowlist = allowlist;
  }


  public DomainAccessRestrictions blocklist(@javax.annotation.Nullable List<String> blocklist) {
    this.blocklist = blocklist;
    return this;
  }

  public DomainAccessRestrictions addBlocklistItem(String blocklistItem) {
    if (this.blocklist == null) {
      this.blocklist = new ArrayList<>();
    }
    this.blocklist.add(blocklistItem);
    return this;
  }

  /**
   * List of blocked domains/emails
   * @return blocklist
   */
  @javax.annotation.Nullable
  public List<String> getBlocklist() {
    return blocklist;
  }

  public void setBlocklist(@javax.annotation.Nullable List<String> blocklist) {
    this.blocklist = blocklist;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the DomainAccessRestrictions instance itself
   */
  public DomainAccessRestrictions putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    DomainAccessRestrictions domainAccessRestrictions = (DomainAccessRestrictions) o;
    return Objects.equals(this.allowlist, domainAccessRestrictions.allowlist) &&
        Objects.equals(this.blocklist, domainAccessRestrictions.blocklist)&&
        Objects.equals(this.additionalProperties, domainAccessRestrictions.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(allowlist, blocklist, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class DomainAccessRestrictions {\n");
    sb.append("    allowlist: ").append(toIndentedString(allowlist)).append("\n");
    sb.append("    blocklist: ").append(toIndentedString(blocklist)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("Allowlist");
    openapiFields.add("Blocklist");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to DomainAccessRestrictions
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!DomainAccessRestrictions.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in DomainAccessRestrictions is not found in the empty JSON string", DomainAccessRestrictions.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("Allowlist") != null && !jsonObj.get("Allowlist").isJsonNull() && !jsonObj.get("Allowlist").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `Allowlist` to be an array in the JSON string but got `%s`", jsonObj.get("Allowlist").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("Blocklist") != null && !jsonObj.get("Blocklist").isJsonNull() && !jsonObj.get("Blocklist").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `Blocklist` to be an array in the JSON string but got `%s`", jsonObj.get("Blocklist").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!DomainAccessRestrictions.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'DomainAccessRestrictions' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<DomainAccessRestrictions> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(DomainAccessRestrictions.class));

       return (TypeAdapter<T>) new TypeAdapter<DomainAccessRestrictions>() {
           @Override
           public void write(JsonWriter out, DomainAccessRestrictions value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public DomainAccessRestrictions read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             DomainAccessRestrictions instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of DomainAccessRestrictions given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of DomainAccessRestrictions
   * @throws IOException if the JSON string is invalid with respect to DomainAccessRestrictions
   */
  public static DomainAccessRestrictions fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, DomainAccessRestrictions.class);
  }

  /**
   * Convert an instance of DomainAccessRestrictions to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}


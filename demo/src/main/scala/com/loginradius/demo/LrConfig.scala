package com.loginradius.demo

object credentials {
	var apiKey = ""
	var apiSecret = ""
}

object urls {
	var resetPasswordUrl = "localhost:8080/resetpassword"
	var emailVerificationUrl = "localhost:8080/emailverification"
}